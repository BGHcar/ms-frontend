import { Ejecucionservicio } from './ejecucionservicio.model';

describe('Ejecucionservicio', () => {
  it('should create an instance', () => {
    expect(new Ejecucionservicio()).toBeTruthy();
  });
});
